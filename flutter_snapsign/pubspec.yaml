name: flutter_snapsign
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^0.13.3
  flutter_slidable: ^0.6.0
  file_picker: ^4.0.0
  image_picker: ^0.8.4+3  # <-- Keep this line
  syncfusion_flutter_pdfviewer: ^20.3.51
  permission_handler: ^10.2.0
  signature: ^5.0.1
  flutter_signature_pad: ^3.0.0
  flutter_pdfview: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/images/